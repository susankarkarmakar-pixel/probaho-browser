--- app/src/App.tsx.bak
+++ app/src/App.tsx
@@ -616,6 +616,11 @@
           case 'inspect':
             if (x !== undefined && y !== undefined) {
               wv.inspectElement(x, y);
             }
             break;
+          case 'copy-image':
+            if (x !== undefined && y !== undefined) {
+              wv.copyImageAt(x, y);
+            }
+            break;
         }
