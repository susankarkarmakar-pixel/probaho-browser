import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.susankar.probahobrowser',
  appName: 'Probaho Browser',
  webDir: 'dist'
};

export default config;
